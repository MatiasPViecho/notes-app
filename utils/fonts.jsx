import { Girassol, Roboto } from 'next/font/google';
export const girassol = Girassol({ subsets: ['latin'], weight: ['400'] });
export const roboto = Roboto({
  subsets: ['latin'],
  weight: ['300', '400', '500'],
});
